# 🔄 Privacy Consent Manager - User Flow Diagrams

## Flow 1: Extension Installation & Setup

```
┌──────────────────────────────────────────────────────────────┐
│                   USER STARTS HERE                            │
└───────────────────────┬──────────────────────────────────────┘
                        ↓
┌───────────────────────────────────────────────────────────────┐
│  Step 1: Download & Install Chrome Extension                  │
│  - Load unpacked in chrome://extensions/                      │
│  - Extension icon appears in toolbar                           │
└───────────────────────┬───────────────────────────────────────┘
                        ↓
┌───────────────────────────────────────────────────────────────┐
│  Step 2: Default Settings Applied                             │
│  ✅ Bot Detection: ON (bots blocked)                          │
│  ✅ Marketing: DENIED                                         │
│  ✅ Analytics: DENIED                                         │
│  ✅ Functional: GRANTED                                       │
│  ✅ Personalization: DENIED                                   │
└───────────────────────┬───────────────────────────────────────┘
                        ↓
┌───────────────────────────────────────────────────────────────┐
│  Step 3: User Ready to Browse!                                │
│  - Extension monitors all page visits                          │
│  - Bot detection active                                        │
│  - Privacy preferences enforced                                │
└───────────────────────────────────────────────────────────────┘
```

---

## Flow 2: Normal User Browsing

```
USER VISITS WEBSITE (e.g., reddit.com)
         │
         ↓
┌─────────────────────────────────────┐
│  Extension Content Script Loads     │
│  (bot-detector.js + content.js)     │
└──────────────┬──────────────────────┘
               ↓
    ┌──────────────────────┐
    │  Check: Bot Blocking │
    │  Enabled?            │
    └──────┬───────────────┘
     YES ↓              ↓ NO (User disabled)
  ┌──────────────┐     │
  │ Run BotD     │     │
  │ Detection    │     │
  └──┬───────────┘     │
     ↓                 │
  ┌──────────────┐     │
  │ Check User   │     │
  │ Agent for    │     │
  │ Whitelist    │     │
  └──┬───────────┘     │
     ↓                 │
  Is Whitelisted?      │
     │                 │
  YES│   NO            │
     ↓   ↓             ↓
  ┌─────────┐  ┌──────────────┐   ┌──────────────┐
  │ ALLOW   │  │ Run BotD     │   │ SKIP         │
  │ (Search │  │ Full Detect  │   │ DETECTION    │
  │ Engine) │  └──┬───────────┘   │ (User choice)│
  └─────────┘     ↓               └──────────────┘
                  │
              Is Bot?
                  │
            ┌─────┴─────┐
          YES           NO
            ↓           ↓
    ┌──────────────┐  ┌──────────────┐
    │ 🚫 BLOCK     │  │ ✅ ALLOW     │
    │ Show Overlay │  │ Normal Page  │
    │              │  │              │
    │ Log Event:   │  │ Apply User   │
    │ - Bot Type   │  │ Consent      │
    │ - URL        │  │ Preferences  │
    │ - Timestamp  │  │              │
    └──────┬───────┘  └──────┬───────┘
           │                 │
           ↓                 ↓
    ┌──────────────┐  ┌──────────────┐
    │ Send to      │  │ Send to      │
    │ Backend API  │  │ Backend API  │
    │              │  │              │
    │ POST /blocked│  │ POST /result │
    └──────┬───────┘  └──────┬───────┘
           │                 │
           └────────┬────────┘
                    ↓
            ┌───────────────────┐
            │ Backend Processes: │
            │ - Save to DB       │
            │ - Update Stats     │
            │ - Create Audit Log │
            └───────────────────┘
```

---

## Flow 3: Bot Detection Process (Detailed)

```
PAGE LOADS
    │
    ↓
┌────────────────────────────────┐
│ bot-detector.js Initializes    │
│ console.log("🤖 Bot Detection  │
│            initialized")        │
└────────────┬───────────────────┘
             ↓
┌────────────────────────────────┐
│ Check Storage:                 │
│ allowBotScraping = ?           │
└────┬───────────────────────────┘
     │
     ├─ FALSE (Default): Continue detection
     └─ TRUE: Skip detection, allow all
     │
     ↓
┌────────────────────────────────┐
│ Step 1: Whitelist Check        │
│ - Get user agent               │
│ - Match against patterns:      │
│   /googlebot/i                 │
│   /bingbot/i                   │
│   /slurp/i                     │
│   ... (20+ patterns)           │
└────┬───────────────────────────┘
     │
     ├─ MATCH FOUND: ✅ Allow (whitelisted)
     └─ NO MATCH: Continue to BotD
     │
     ↓
┌────────────────────────────────┐
│ Step 2: Load BotD Library      │
│ - Import from CDN:             │
│   https://openfpcdn.io/botd/v2 │
│ - Wait for load (max 10s)      │
│ - If fails: Use fallback       │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 3: Run BotD Detection     │
│ const botd = await BotD.load() │
│ const result = await           │
│   botd.detect()                │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 4: Analyze Result         │
│ result = {                     │
│   bot: true/false,             │
│   botKind: "Selenium",         │
│   requestTime: 123             │
│ }                              │
└────┬───────────────────────────┘
     │
     ├─ result.bot === FALSE
     │  ↓
     │  ✅ Human User
     │  - Allow page access
     │  - Log as human
     │  - Update stats
     │
     └─ result.bot === TRUE
        ↓
        🚫 Bot Detected
        │
        ↓
┌────────────────────────────────┐
│ Step 5: Create Blocking Overlay│
│ - Full-page purple gradient    │
│ - White card with 🤖 emoji     │
│ - "Automated Access Detected"  │
│ - Show bot details:            │
│   * Bot Type                   │
│   * Detection Method           │
│   * Timestamp                  │
│ - Contact support option       │
│ - Cannot be dismissed          │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 6: Send to Backend        │
│ chrome.runtime.sendMessage({   │
│   action: 'botBlocked',        │
│   result: {...},               │
│   url: window.location.href,   │
│   timestamp: new Date()        │
│ })                             │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 7: Background Script      │
│ - Receives message             │
│ - Calls API:                   │
│   POST /api/bot-detection/     │
│        blocked                 │
│ - Increments stats counter     │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 8: Backend Processing     │
│ - Validate API key             │
│ - Save to DB:                  │
│   botd_blocked_events          │
│ - Update statistics:           │
│   botd_statistics              │
│ - Create audit log             │
│ - Return success               │
└────────────────────────────────┘
```

---

## Flow 4: User Toggling Bot Detection

```
USER CLICKS EXTENSION ICON
         │
         ↓
┌───────────────────────────────┐
│  Popup Opens (popup.html)     │
│  - Shows current settings     │
│  - Bot toggle is OFF (green)  │
└─────────┬─────────────────────┘
          ↓
USER CLICKS "Allow AI/Bot Scraping" TOGGLE
          │
          ↓
┌───────────────────────────────┐
│  JavaScript Event Handler     │
│  (popup.js)                   │
│                               │
│  allowBotScraping = TRUE      │
└─────────┬─────────────────────┘
          ↓
┌───────────────────────────────┐
│  Step 1: Update Local Storage │
│  chrome.storage.local.set({   │
│    allowBotScraping: true     │
│  })                           │
└─────────┬─────────────────────┘
          ↓
┌───────────────────────────────┐
│  Step 2: Update UI            │
│  - Change indicator: Yellow   │
│  - Text: "Bots Allowed"       │
│  - Detail: "AI and bots can   │
│    scrape content"            │
└─────────┬─────────────────────┘
          ↓
┌───────────────────────────────┐
│  Step 3: Show Notification    │
│  showNotification(            │
│    "🤖 Bot scraping allowed"  │
│  )                            │
└─────────┬─────────────────────┘
          ↓
┌───────────────────────────────┐
│  Step 4: Broadcast to All Tabs│
│  chrome.runtime.sendMessage({ │
│    action: 'updateBotDetect', │
│    allowBotScraping: true     │
│  })                           │
└─────────┬─────────────────────┘
          ↓
┌───────────────────────────────┐
│  Step 5: Content Scripts      │
│  Receive Message              │
│  - If blocking overlay exists:│
│    * Remove it                │
│    * Restore page access      │
│  - Future page loads:         │
│    * Skip bot detection       │
│    * Allow all access         │
└───────────────────────────────┘
```

---

## Flow 5: Backend Data Processing

```
API REQUEST ARRIVES
    │
    ↓
┌────────────────────────────────┐
│ Next.js API Route              │
│ /api/bot-detection/result      │
│ or /api/bot-detection/blocked  │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 1: Validate API Key       │
│ const org = await              │
│   validateApiKey(apiKey)       │
│                                │
│ If invalid: Return 401         │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 2: Parse Request Body     │
│ const { bot_detected,          │
│         bot_kind,              │
│         url, ... } = body      │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 3: Call Service Layer     │
│ const result = await           │
│   logBotDetectionResult({      │
│     organization_id,           │
│     bot_detected,              │
│     bot_kind,                  │
│     url, ...                   │
│   })                           │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 4: Database Insert        │
│ INSERT INTO                    │
│   botd_detection_results       │
│ VALUES (...)                   │
│ RETURNING *                    │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 5: Update Statistics      │
│ (Async - doesn't block)        │
│                                │
│ INSERT INTO botd_statistics    │
│ VALUES (org, date, hour, ...)  │
│ ON CONFLICT (org, date, hour)  │
│ DO UPDATE SET                  │
│   total_detections = +1,       │
│   bots_detected = +1 (if bot)  │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 6: Create Audit Log       │
│ await createAuditLog(          │
│   org_id,                      │
│   'bot_detection',             │
│   'botd_detection_result',     │
│   result.id,                   │
│   {...}                        │
│ )                              │
└────┬───────────────────────────┘
     │
     ↓
┌────────────────────────────────┐
│ Step 7: Return Response        │
│ return {                       │
│   success: true,               │
│   data: {                      │
│     id: result.id,             │
│     logged: true,              │
│     timestamp: ...             │
│   }                            │
│ }                              │
└────────────────────────────────┘
```

---

## Summary: Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER'S BROWSER                            │
│                                                                  │
│  ┌──────────────────┐         ┌─────────────────────────┐      │
│  │ Chrome Extension │────────▶│  Webpage                │      │
│  │                  │         │  - reddit.com           │      │
│  │ - popup.html     │         │  - twitter.com          │      │
│  │ - popup.js       │         │  - any site             │      │
│  │ - background.js  │         └─────────┬───────────────┘      │
│  └────────┬─────────┘                   │                       │
│           │                             │                       │
│           │  ┌──────────────────────────┘                       │
│           │  │                                                  │
│           ↓  ↓                                                  │
│  ┌──────────────────┐                                           │
│  │ Content Scripts  │                                           │
│  │ - bot-detector.js│                                           │
│  │ - content.js     │                                           │
│  └────────┬─────────┘                                           │
│           │                                                      │
│           ↓                                                      │
│  ┌──────────────────┐                                           │
│  │ BotD Library     │                                           │
│  │ (from CDN)       │                                           │
│  │ Detection Engine │                                           │
│  └────────┬─────────┘                                           │
└───────────┼──────────────────────────────────────────────────────┘
            │
            │ HTTP POST
            │ (Detection Results)
            ↓
┌─────────────────────────────────────────────────────────────────┐
│                        BACKEND SERVER                            │
│                        (Next.js)                                 │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐       │
│  │  API Routes                                           │       │
│  │  - /api/bot-detection/result  (POST)                 │       │
│  │  - /api/bot-detection/blocked (POST)                 │       │
│  │  - /api/bot-detection/blocked (GET stats)            │       │
│  │  - /api/consent/*                                    │       │
│  └────────┬─────────────────────────────────────────────┘       │
│           │                                                      │
│           ↓                                                      │
│  ┌──────────────────┐                                           │
│  │ Service Layer    │                                           │
│  │ - botd-service.ts│                                           │
│  │ - auth.ts        │                                           │
│  └────────┬─────────┘                                           │
│           │                                                      │
│           ↓                                                      │
│  ┌──────────────────┐                                           │
│  │ Database Client  │                                           │
│  │ (Neon Serverless)│                                           │
│  └────────┬─────────┘                                           │
└───────────┼──────────────────────────────────────────────────────┘
            │
            │ SQL Queries
            ↓
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE (PostgreSQL)                       │
│                                                                  │
│  Tables:                                                         │
│  - botd_detection_results  (all detection events)               │
│  - botd_blocked_events     (all blocking events)                │
│  - bot_whitelist           (whitelisted user agents/IPs)        │
│  - botd_statistics         (hourly aggregated stats)            │
│  - consent_records         (user consent data)                  │
│  - organizations           (API keys, settings)                 │
│  - audit_logs              (all system events)                  │
└─────────────────────────────────────────────────────────────────┘
```

---

**This is your complete system!** 🎉

