import type React from "react"
export const metadata = {
  title: "API Keys - Admin",
  description: "View and copy API keys for testing",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
